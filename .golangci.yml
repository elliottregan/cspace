version: "2"

run:
  timeout: 3m

linters:
  default: standard
  enable:
    - misspell
  exclusions:
    paths:
      - internal/assets/embedded

formatters:
  enable:
    - gofmt
    - goimports
  exclusions:
    paths:
      - internal/assets/embedded
